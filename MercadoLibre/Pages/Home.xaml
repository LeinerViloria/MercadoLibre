<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MercadoLibre.Pages.Home">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Buscar en mercado libre" />
        <ToolbarItem IconImageSource="cart_shop.png" />
    </ContentPage.ToolbarItems>

    <VerticalStackLayout>
        <HorizontalStackLayout HeightRequest="30" Background="{StaticResource Primary}" Padding="14, 0, 14, 0">
            <Image Source="gps.png" HeightRequest="18" VerticalOptions="Start" />
            <Label Text="Dirección tal, con calle x" />
            <Image Source="right.png" HeightRequest="20" VerticalOptions="Start" />
        </HorizontalStackLayout>

        <ScrollView>
            <VerticalStackLayout>
                <CarouselView />

                <Image Source="suscription_home.png" WidthRequest="350" />
                <Image Source="envio_gratis.png" WidthRequest="350" />
            </VerticalStackLayout>
        </ScrollView>
    </VerticalStackLayout>
    
</ContentPage>