<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MercadoLibre.Models"
             x:Class="MercadoLibre.Pages.NavBar"
             Title="NavBar"
             BackgroundColor="White">
    <ScrollView>
        <VerticalStackLayout>

            <CollectionView ItemsSource="{Binding Items}" SelectionMode="Single" Margin="20, 0, 0, 0">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Menu" >

                        <StackLayout>
                            <Label Text="{Binding Name}" IsVisible="{Binding ShowInfo}" TextColor="Black" />

                            <BoxView IsVisible="{Binding IsSeparator}" HeightRequest="1" Color="Gray"/>
                        </StackLayout>
                        
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>